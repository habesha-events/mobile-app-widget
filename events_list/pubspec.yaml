name: events_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.18+8

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  provider: ^6.0.3
  location: ^4.3.0
  cached_network_image: ^3.2.3
  shared_preferences: ^2.0.9
  geocoding: ^2.0.4
  url_launcher: ^6.2.2


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/events_api_response.json
